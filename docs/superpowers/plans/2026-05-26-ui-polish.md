# UI Polish & Animation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply warm cream theme, CSS animations, surface polish, and JS micro-interactions to `index.html` — no new dependencies, no HTML structure changes, no build step.

**Architecture:** Four sequential tasks, each confined to `index.html`. Tasks 1–3 are CSS-only edits inside `<style>`. Task 4 is JS-only edits inside `<script>`. Each task is independently verifiable by reloading the browser.

**Tech Stack:** Vanilla CSS (custom properties, `@keyframes`, `transition`), Vanilla JS (`requestAnimationFrame`, `animationend` event), single HTML file.

---

### Task 1: CSS Token Swap (Colour System)

**Files:**
- Modify: `index.html` — `:root` block inside `<style>` (~line 11)

- [ ] **Step 1: Replace `:root` colour tokens**

Find this exact block:
```css
:root{
  --red:#E8192C; --dark:#090909; --surface:#111111; --card:#161616;
  --card2:#1c1c1c; --border:#252525; --border2:#303030; --muted:#4a4a4a;
  --text:#EDEAE5; --sub:#777777; --green:#1DB954; --amber:#F5A623;
  --blue:#4A9EFF; --purple:#9B59B6;
  --font-display:'Syne',sans-serif; --font-mono:'DM Mono',monospace;
}
```

Replace with:
```css
:root{
  --red:#C8102E; --dark:#F5F0E8; --surface:#EDE8DF; --card:#FDFAF5;
  --card2:#E8E2D8; --border:#D8D0C4; --border2:#C8BEB0; --muted:#A8A090;
  --text:#1C1410; --sub:#7A6E62; --green:#2A7A45; --amber:#B86A10;
  --blue:#1A4E8C; --purple:#6A3890;
  --font-display:'Syne',sans-serif; --font-mono:'DM Mono',monospace;
}
```

- [ ] **Step 2: Verify in browser**

Open `index.html`. Confirm:
- Page background is warm cream (`#F5F0E8`), not black
- Header remains dark (it uses hardcoded `rgba(9,9,9,0.92)` — untouched)
- Upload cards are off-white (`#FDFAF5`)
- "Continue" button is deep Levi's red (`#C8102E`)
- All body text is dark and readable

- [ ] **Step 3: Commit**
```bash
git add index.html
git commit -m "Apply warm cream colour token swap"
```

---

### Task 2: CSS Animations & Keyframes

**Files:**
- Modify: `index.html` — `<style>` block

#### 2a — Panel spring easing

- [ ] **Step 1: Update `fadeUp` animation easing**

Find:
```css
.panel.active{display:block;animation:fadeUp 0.28s ease}
```

Replace with:
```css
.panel.active{display:block;animation:fadeUp 0.28s cubic-bezier(0.22,1,0.36,1)}
```

#### 2b — Upload card hover lift

- [ ] **Step 2: Add `transform` and `box-shadow` to upload card transition**

Find:
```css
.upload-card{
  background:var(--card);border:1.5px dashed var(--border2);border-radius:6px;
  padding:24px;cursor:pointer;transition:border-color 0.2s,background 0.2s;
  position:relative;min-height:200px;display:flex;flex-direction:column;
}
```

Replace with:
```css
.upload-card{
  background:var(--card);border:1.5px dashed var(--border2);border-radius:6px;
  padding:24px;cursor:pointer;transition:border-color 0.2s,background 0.2s,transform 0.2s,box-shadow 0.2s;
  position:relative;min-height:200px;display:flex;flex-direction:column;
}
```

- [ ] **Step 3: Add hover lift styles**

Find:
```css
.upload-card:hover{border-color:var(--border2);background:var(--card2)}
```

Replace with:
```css
.upload-card:hover{border-color:var(--border2);background:var(--card2);transform:translateY(-2px);box-shadow:0 4px 16px rgba(28,20,16,0.08)}
```

#### 2c — Keyframes: shimmer, shake, pulse-border

- [ ] **Step 4: Add three new `@keyframes` blocks**

Find:
```css
@keyframes fadeUp{from{opacity:0;transform:translateY(14px)}to{opacity:1;transform:translateY(0)}}
```

Replace with:
```css
@keyframes fadeUp{from{opacity:0;transform:translateY(14px)}to{opacity:1;transform:translateY(0)}}
@keyframes shimmer{
  0%{transform:translateX(-100%);opacity:0.6}
  100%{transform:translateX(250%);opacity:0}
}
@keyframes shake{
  0%,100%{transform:translateX(0)}
  15%{transform:translateX(-6px)}
  35%{transform:translateX(6px)}
  55%{transform:translateX(-4px)}
  75%{transform:translateX(4px)}
  90%{transform:translateX(-2px)}
}
@keyframes pulse-border{
  0%,100%{box-shadow:0 0 0 0 rgba(200,16,46,0)}
  50%{box-shadow:0 0 0 5px rgba(200,16,46,0.2)}
}
```

- [ ] **Step 5: Add shimmer overlay on loaded cards and shake class**

Find:
```css
.upload-card.error-state{border-color:var(--red);border-style:solid}
```

Add immediately after that line:
```css
.upload-card.loaded::after{
  content:'';position:absolute;top:0;left:0;width:35%;height:100%;
  background:linear-gradient(90deg,transparent,rgba(42,122,69,0.1),transparent);
  animation:shimmer 0.65s ease-out forwards;pointer-events:none;border-radius:inherit;
}
.upload-card.shake{animation:shake 0.4s ease}
```

#### 2d — Step bar active accent

- [ ] **Step 6: Update active step to use inset box-shadow accent instead of just background**

Find:
```css
.step-item.active{background:rgba(232,25,44,0.08)}
.step-item.done{background:rgba(29,185,84,0.05)}
```

Replace with:
```css
.step-item.active{background:rgba(200,16,46,0.06);box-shadow:inset 3px 0 0 var(--red)}
.step-item.done{background:rgba(42,122,69,0.05)}
```

#### 2e — Input focus ring

- [ ] **Step 7: Add `box-shadow` to text input transition and update focus style**

Find:
```css
.text-input{
  width:100%;background:var(--card);border:1px solid var(--border2);
  border-radius:3px;padding:9px 12px;color:var(--text);font-size:12px;
  transition:border-color 0.15s;
}
.text-input:focus{border-color:var(--sub)}
```

Replace with:
```css
.text-input{
  width:100%;background:var(--card);border:1px solid var(--border2);
  border-radius:3px;padding:9px 12px;color:var(--text);font-size:12px;
  transition:border-color 0.15s,box-shadow 0.15s;
}
.text-input:focus{border-color:var(--red);box-shadow:0 2px 0 var(--red)}
```

#### 2f — Priority list hover accent bar

- [ ] **Step 8: Add `::before` pseudo-element to priority items**

Find:
```css
.priority-item{
  display:flex;align-items:center;gap:12px;padding:10px 16px;
  border-bottom:1px solid var(--border);
  transition:background 0.15s;
}
```

Replace with:
```css
.priority-item{
  display:flex;align-items:center;gap:12px;padding:10px 16px;
  border-bottom:1px solid var(--border);
  transition:background 0.15s;position:relative;
}
.priority-item::before{
  content:'';position:absolute;left:0;top:0;bottom:0;
  width:3px;background:var(--red);
  transform:scaleY(0);transition:transform 0.15s ease;transform-origin:center;
}
.priority-item:hover::before{transform:scaleY(1)}
```

#### 2g — Progress bar glow + run button pulse class

- [ ] **Step 9: Add running glow to progress bar and pulsing class to run button**

Find:
```css
.progress-bar{height:3px;background:var(--red);border-radius:2px;width:0%;transition:width 0.5s ease}
```

Replace with:
```css
.progress-bar{height:3px;background:var(--red);border-radius:2px;width:0%;transition:width 0.5s ease}
.progress-bar.running{box-shadow:2px 0 8px var(--red),0 0 4px rgba(200,16,46,0.4)}
#runBtn.pulsing{animation:pulse-border 2s ease-in-out infinite}
```

#### 2h — Toast bounce easing

- [ ] **Step 10: Update toast transition to use spring curve on entry**

Find:
```css
#toast{
  position:fixed;bottom:28px;right:28px;z-index:9999;
  background:var(--card2);border-radius:4px;padding:12px 18px;
  font-size:11px;letter-spacing:0.05em;max-width:320px;
  opacity:0;transform:translateY(8px);transition:all 0.25s;pointer-events:none;
}
```

Replace with:
```css
#toast{
  position:fixed;bottom:28px;right:28px;z-index:9999;
  background:var(--card2);border-radius:4px;padding:12px 18px;
  font-size:11px;letter-spacing:0.05em;max-width:320px;
  opacity:0;transform:translateY(8px);
  transition:opacity 0.2s ease,transform 0.35s cubic-bezier(0.34,1.56,0.64,1);
  pointer-events:none;
}
```

#### 2i — Primary button flash on click

- [ ] **Step 11: Add `::after` flash overlay to primary button**

Find:
```css
.btn-primary{
  font-family:var(--font-display);font-weight:700;font-size:13px;
  letter-spacing:0.08em;text-transform:uppercase;
  padding:12px 32px;background:var(--red);color:#fff;border-radius:3px;
  transition:all 0.15s;
}
```

Replace with:
```css
.btn-primary{
  font-family:var(--font-display);font-weight:700;font-size:13px;
  letter-spacing:0.08em;text-transform:uppercase;
  padding:12px 32px;background:var(--red);color:#fff;border-radius:3px;
  transition:all 0.15s;position:relative;overflow:hidden;
}
.btn-primary::after{
  content:'';position:absolute;inset:0;
  background:rgba(255,255,255,0);
  transition:background 0.3s ease;pointer-events:none;
}
.btn-primary:active::after{background:rgba(255,255,255,0.18);transition:0s}
```

- [ ] **Step 12: Verify all CSS animations in browser**

Open `index.html`. Check:
- Step panel appears with a slight spring overshoot
- Upload card lifts 2px on hover
- Active step in step bar has a red left inset accent
- Clicking any input shows a red bottom-line focus indicator
- Priority list rows show a red left bar on hover
- Clicking a primary button flashes white briefly
- Toast pops in with a subtle bounce

- [ ] **Step 13: Commit**
```bash
git add index.html
git commit -m "Add CSS animations: spring transitions, card hover, button flash, priority accent, input focus, keyframes"
```

---

### Task 3: Surface Polish

**Files:**
- Modify: `index.html` — `<style>` block (shadows, border widths, scrollbar)

- [ ] **Step 1: Add elevation shadow to upload cards**

Find:
```css
.upload-card{
  background:var(--card);border:1.5px dashed var(--border2);border-radius:6px;
  padding:24px;cursor:pointer;transition:border-color 0.2s,background 0.2s,transform 0.2s,box-shadow 0.2s;
  position:relative;min-height:200px;display:flex;flex-direction:column;
}
```

Replace with:
```css
.upload-card{
  background:var(--card);border:1.5px dashed var(--border2);border-radius:6px;
  padding:24px;cursor:pointer;transition:border-color 0.2s,background 0.2s,transform 0.2s,box-shadow 0.2s;
  position:relative;min-height:200px;display:flex;flex-direction:column;
  box-shadow:0 1px 4px rgba(28,20,16,0.07);
}
```

- [ ] **Step 2: Thicken stat card top strip from 2px → 3px and add shadow**

Find:
```css
.stat-card{background:var(--card);border:1px solid var(--border);border-radius:4px;padding:20px;position:relative;overflow:hidden}
.stat-card::before{content:'';position:absolute;top:0;left:0;right:0;height:2px}
```

Replace with:
```css
.stat-card{background:var(--card);border:1px solid var(--border);border-radius:4px;padding:20px;position:relative;overflow:hidden;box-shadow:0 2px 8px rgba(28,20,16,0.06)}
.stat-card::before{content:'';position:absolute;top:0;left:0;right:0;height:3px}
```

- [ ] **Step 3: Add shadow to download cards**

Find:
```css
.dl-card{background:var(--card);border:1px solid var(--border);border-radius:4px;padding:24px}
```

Replace with:
```css
.dl-card{background:var(--card);border:1px solid var(--border);border-radius:4px;padding:24px;box-shadow:0 2px 8px rgba(28,20,16,0.06)}
```

- [ ] **Step 4: Strengthen table header bottom border**

Find:
```css
th{padding:9px 12px;text-align:left;font-size:9px;letter-spacing:0.15em;text-transform:uppercase;color:var(--sub);border-bottom:1px solid var(--border2);white-space:nowrap}
```

Replace with:
```css
th{padding:9px 12px;text-align:left;font-size:9px;letter-spacing:0.15em;text-transform:uppercase;color:var(--sub);border-bottom:2px solid var(--border2);white-space:nowrap}
```

- [ ] **Step 5: Update scrollbar colours for cream theme**

Find:
```css
::-webkit-scrollbar{width:4px;height:4px}
::-webkit-scrollbar-track{background:var(--dark)}
::-webkit-scrollbar-thumb{background:var(--muted);border-radius:2px}
::-webkit-scrollbar-thumb:hover{background:var(--sub)}
```

Replace with:
```css
::-webkit-scrollbar{width:4px;height:4px}
::-webkit-scrollbar-track{background:var(--surface)}
::-webkit-scrollbar-thumb{background:var(--border2);border-radius:2px}
::-webkit-scrollbar-thumb:hover{background:var(--muted)}
```

- [ ] **Step 6: Verify surface polish in browser**

Open `index.html`. Check:
- Upload cards have a subtle shadow (look slightly elevated)
- Stat card top coloured strips are visibly thicker
- Table column headers have a bolder bottom separator line
- Scrollbars on the priority list blend into the cream background

- [ ] **Step 7: Commit**
```bash
git add index.html
git commit -m "Surface polish: card shadows, stat strip, table headers, scrollbar"
```

---

### Task 4: JS Micro-interactions

**Files:**
- Modify: `index.html` — `<script>` block

#### 4a — Stat Counter Animation

- [ ] **Step 1: Add `animateCounter` helper after `today()`**

Find:
```javascript
function today() {
  return new Date().toISOString().slice(0,10);
}
```

Replace with:
```javascript
function today() {
  return new Date().toISOString().slice(0,10);
}

/* ─── STAT COUNTER ───────────────────────────────────── */
function animateCounter(el, target, duration) {
  const start = performance.now();
  function tick(now) {
    const elapsed = now - start;
    const progress = Math.min(elapsed / duration, 1);
    const ease = 1 - Math.pow(1 - progress, 3); // easeOutCubic
    const current = Math.round(target * ease);
    el.textContent = current.toLocaleString();
    if(progress < 1) requestAnimationFrame(tick);
    else el.textContent = target.toLocaleString();
  }
  requestAnimationFrame(tick);
}
```

- [ ] **Step 2: Replace direct textContent assignments in `renderResults` with `animateCounter`**

Find:
```javascript
function renderResults(lotTotal, allocated, unallocTotal, retailersServed) {
  document.getElementById('statTotal').textContent = lotTotal.toLocaleString();
  document.getElementById('statAlloc').textContent = allocated.toLocaleString();
  document.getElementById('statUnalloc').textContent = unallocTotal.toLocaleString();
  document.getElementById('statRetailers').textContent = retailersServed;
```

Replace with:
```javascript
function renderResults(lotTotal, allocated, unallocTotal, retailersServed) {
  animateCounter(document.getElementById('statTotal'), lotTotal, 800);
  animateCounter(document.getElementById('statAlloc'), allocated, 800);
  animateCounter(document.getElementById('statUnalloc'), unallocTotal, 800);
  animateCounter(document.getElementById('statRetailers'), retailersServed, 600);
```

#### 4b — Log Line Slide-in

- [ ] **Step 3: Modify `log()` to animate each line in from the left**

Find:
```javascript
function log(type, msg) {
  const box = document.getElementById('logBox');
  box.style.display = 'block';
  const ts = new Date().toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false});
  const icon = {ok:'✓',warn:'⚠',err:'✗',info:'→'}[type]||'·';
  const line = document.createElement('div');
  line.className = 'log-line ' + type;
  line.innerHTML = `<span class="log-ts">${ts}</span><span class="log-msg">${icon} ${msg}</span>`;
  box.appendChild(line);
  box.scrollTop = box.scrollHeight;
}
```

Replace with:
```javascript
function log(type, msg) {
  const box = document.getElementById('logBox');
  box.style.display = 'block';
  const ts = new Date().toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:false});
  const icon = {ok:'✓',warn:'⚠',err:'✗',info:'→'}[type]||'·';
  const line = document.createElement('div');
  line.className = 'log-line ' + type;
  line.innerHTML = `<span class="log-ts">${ts}</span><span class="log-msg">${icon} ${msg}</span>`;
  line.style.cssText = 'opacity:0;transform:translateX(-8px);transition:opacity 0.18s ease,transform 0.18s ease';
  box.appendChild(line);
  box.scrollTop = box.scrollHeight;
  requestAnimationFrame(() => { line.style.opacity='1'; line.style.transform='translateX(0)'; });
}
```

#### 4c — Upload Card Error Shake

- [ ] **Step 4: Modify `setFileStatus` to trigger shake on error**

Find:
```javascript
function setFileStatus(key, type, msg) {
  const el = document.getElementById('stat-'+key);
  el.className = 'file-status ' + type;
  el.textContent = msg;
  const card = document.getElementById('card-'+key);
  card.classList.remove('loaded','error-state');
  if(type==='ok') card.classList.add('loaded');
  if(type==='err') card.classList.add('error-state');
}
```

Replace with:
```javascript
function setFileStatus(key, type, msg) {
  const el = document.getElementById('stat-'+key);
  el.className = 'file-status ' + type;
  el.textContent = msg;
  const card = document.getElementById('card-'+key);
  card.classList.remove('loaded','error-state','shake');
  if(type==='ok') card.classList.add('loaded');
  if(type==='err') {
    card.classList.add('error-state');
    requestAnimationFrame(() => {
      card.classList.add('shake');
      card.addEventListener('animationend', () => card.classList.remove('shake'), {once:true});
    });
  }
}
```

#### 4d — Run Button Pulse + Progress Bar Glow

- [ ] **Step 5: Add `.pulsing` and `.running` when allocation starts**

Find:
```javascript
function runAllocation() {
  // Validation
  if(!state.lotName) {
    if(!confirm('Lot number is empty. Continue anyway?')) return;
  }
  const runBtn = document.getElementById('runBtn');
  runBtn.disabled = true;
  clearLog();
  setProgress(0);
  setTimeout(executeAllocation, 80);
}
```

Replace with:
```javascript
function runAllocation() {
  // Validation
  if(!state.lotName) {
    if(!confirm('Lot number is empty. Continue anyway?')) return;
  }
  const runBtn = document.getElementById('runBtn');
  runBtn.disabled = true;
  runBtn.classList.add('pulsing');
  document.getElementById('progressBar').classList.add('running');
  clearLog();
  setProgress(0);
  setTimeout(executeAllocation, 80);
}
```

- [ ] **Step 6: Remove `.pulsing` and `.running` on successful completion**

Find:
```javascript
    // Render results and navigate
    setTimeout(()=>{
      markDone(2); markDone(3);
      renderResults(lotTotal, totalAllocated, unallocatedResults.reduce((s,r)=>s+r.remaining,0), retailersServed);
      goToStep(4);
    }, 600);
```

Replace with:
```javascript
    // Stop pulse + glow
    document.getElementById('runBtn').classList.remove('pulsing');
    document.getElementById('progressBar').classList.remove('running');
    // Render results and navigate
    setTimeout(()=>{
      markDone(2); markDone(3);
      renderResults(lotTotal, totalAllocated, unallocatedResults.reduce((s,r)=>s+r.remaining,0), retailersServed);
      goToStep(4);
    }, 600);
```

- [ ] **Step 7: Remove `.pulsing` and `.running` on error**

Find:
```javascript
  } catch(err) {
    log('err', 'Engine error: ' + err.message);
    showToast('Allocation failed: ' + err.message, 'err');
    document.getElementById('runBtn').disabled = false;
    setProgress(0);
    console.error(err);
  }
```

Replace with:
```javascript
  } catch(err) {
    log('err', 'Engine error: ' + err.message);
    showToast('Allocation failed: ' + err.message, 'err');
    const runBtnErr = document.getElementById('runBtn');
    runBtnErr.disabled = false;
    runBtnErr.classList.remove('pulsing');
    document.getElementById('progressBar').classList.remove('running');
    setProgress(0);
    console.error(err);
  }
```

- [ ] **Step 8: Clean up pulse/glow state in `resetAll()`**

Find:
```javascript
  document.getElementById('runBtn').disabled = false;
  // Reset step bar
```

Replace with:
```javascript
  const runBtnReset = document.getElementById('runBtn');
  runBtnReset.disabled = false;
  runBtnReset.classList.remove('pulsing');
  document.getElementById('progressBar').classList.remove('running');
  // Reset step bar
```

- [ ] **Step 9: Verify all JS micro-interactions**

Run allocation with your real data. Verify:
1. **Stat counters** — numbers on results screen animate from 0 upward (~800ms)
2. **Log slide-in** — each log line slides in from the left as allocation runs
3. **Card shake** — upload a corrupt file; the card shakes side-to-side, then stops; works again on re-upload
4. **Run button pulse** — red glow pulses on the button while engine runs, stops when results appear
5. **Progress bar glow** — red edge glow visible as bar fills; removed when done

- [ ] **Step 10: Commit**
```bash
git add index.html
git commit -m "Add JS micro-interactions: stat counters, log slide-in, card shake, run button pulse"
```

---

### Task 5: Push & Verify Deployment

- [ ] **Step 1: Push to GitHub**
```bash
git push
```

- [ ] **Step 2: Verify on GitHub Pages (~1 min after push)**

Open `https://okalfalfa580.github.io/Allocation-Engine/` and confirm:
- Warm cream theme visible
- Dark header intact
- Upload cards lift on hover
- All animations functional
